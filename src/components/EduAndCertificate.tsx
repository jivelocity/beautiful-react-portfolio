
interface EduAndCertificateProps {
  className?:string
}

export const EduAndCertificate: React.FC<EduAndCertificateProps> = ({
    className
 }) => {
  return (
    <div className={`${className}`}>
     EduAndCertificate
    </div>
  )
}
